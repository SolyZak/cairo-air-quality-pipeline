[pytest]
testpaths = tests
# Put the repo root on sys.path so `import ingestion` works without installing
# the package. The alternative is a pyproject.toml with a build backend, which
# is more machinery than a four-module package needs.
pythonpath = .
addopts = -q --strict-markers
